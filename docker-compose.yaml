version: '3'
services:
  agent_loop:
    build:
      context: .
      dockerfile: agent_loop/Dockerfile
  kernel_quantum:
    build:
      context: .
      dockerfile: kernel_quantum/Dockerfile
  kernel_unknown_engine:
    build:
      context: .
      dockerfile: kernel_unknown_engine/Dockerfile
  federated_trainer:
    build:
      context: .
      dockerfile: rl_core/Dockerfile
  dashboard:
    build:
      context: .
      dockerfile: dashboard/Dockerfile
    ports:
      - "8000:8000"
