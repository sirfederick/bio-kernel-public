# requirements-dev.txt

# --- Dependencias existentes (mantener) ---
biopython
pyyaml
colorama
pandas                     # Procesamiento de datos tabulares
requests                   # Cliente HTTP simple para APIs
lief                       # Análisis binario liviano
scikit-learn               # Algoritmos de clustering y ML
pydantic-settings          # Gestión de configuraciones vía env

# --- Dependencias principales para motor y núcleo ---
pydantic>=2.6              # Manejo robusto de configuraciones
pydantic-core>=2.6         # Núcleo nativo para pydantic
loguru>=0.7                # Logging avanzado y estructurado
faiss-cpu>=1.8             # Índice vectorial FAISS (CPU)
numpy>=1.26                # Soporte FAISS y cálculos numéricos
ray>=2.10                  # Ejecución distribuida y orquestación de agentes
torch                      # Soporte GPU opcional para ML
sentence_transformers      # Embeddings locales de alta calidad
httpx>=0.27                # Cliente HTTP async para APIs externas
langchain>=0.2.0           # Framework para RAG y pipelines (opcional)
langchain-community>=0.3.27 # Extensiones y mejoras para langchain

# --- Configuración y validación ---
python-decouple>=3.8       # Gestión segura de variables de entorno

# --- Extras para UI, APIs y seguridad ---
fastapi                   # Framework para APIs REST y WebSocket (dashboard)
uvicorn[standard]         # Servidor ASGI para FastAPI y websockets
tenacity                  # Retry logic robusta para llamadas API y operaciones
pyzmq                     # Messaging backend used by kernel_quantum.messaging

# --- Paquetes base para compilación y soporte nativo ---
pip>=23.0
setuptools>=65.0
wheel>=0.38
openai>=1.0
langchain_openai
